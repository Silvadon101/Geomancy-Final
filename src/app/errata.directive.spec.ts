import { ErrataDirective } from './errata.directive';

describe('ErrataDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrataDirective();
    expect(directive).toBeTruthy();
  });
});
